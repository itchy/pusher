<div id='topics-bin'>
  <p>
    <b>Topics Bin<b/>
      <ul>
        <% topics.each do |topic| %>
          <li <%= "class=new" if topic.new? %> ><%= topic.title %></li>
        <% end %>
      </ul>
  </p>
</div>