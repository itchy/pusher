<div id='topics-bin'>
  <p>
    <b>Topics Bin<b/>
      <ul>
        <% topics.each do |topic| %>
          <li <%= "class=new" if topic.new? %> ><%= topic.title %> <%= link_to "Discuss", activate_path(topic), :remote => true %></li>
        <% end %>
      </ul>
  </p>
</div>